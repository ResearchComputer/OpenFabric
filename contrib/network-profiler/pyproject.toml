[project]
name = "network-profiler"
version = "0.1.0"
description = "Pairwise network profiling over remote-cluster-controller managed machines"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
network-profiler = "network_profiler.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
