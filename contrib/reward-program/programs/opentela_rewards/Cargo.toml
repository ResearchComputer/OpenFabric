[package]
name = "opentela_rewards"
version = "0.1.0"
description = "OpenTela on-chain reward protocol"
edition = "2021"
license = "MIT"

[lib]
crate-type = ["cdylib", "lib"]
name = "opentela_rewards"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build", "anchor-spl/idl-build"]

[dependencies]
anchor-lang = { version = "0.31.1", features = ["init-if-needed"] }
anchor-spl = "0.31.1"
